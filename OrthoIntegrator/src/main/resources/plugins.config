# A single .jar file can contain multiple plugins, specified in separate lines.
#
# The format is: <menu>, "<menu label>", <class name>
#
# If something like ("<arg>") is appended to the class name, the setup() method
# will get that as arg parameter; otherwise arg is simply the empty string.

NordenTools, "Concentration Projection (onto line)",    ConcentrationAlongLine_
NordenTools, "Section Area (perpendicular to line)",    SectionAreaAlongLine_
NordenTools, "Filter Hull",                             HullFilter_
NordenTools, "Shape Approximation",                     ShapeApprox_
NordenTools, "Manual 4D Crop",                          InterpolatedManualCrop_
